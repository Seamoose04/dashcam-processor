# ============================================================
# Workhorse configuration — match these to your hardware.
# See docs/devices/workhorse.md for what each value controls.
#
# Any key left at its sentinel (0 / "") triggers auto-detection from
# hardware, so this file is optional. Keys are case-sensitive; one per line.
# ============================================================

max_workers = 64
available_vram_gb = 20.0
log_level = info
log_dir = logs
